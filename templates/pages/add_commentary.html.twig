{% extends 'base.html.twig' %}

{% block title %}Hello AddCommentaryController!
{% endblock %}

{% block body %}
	{{ form_start(form) }}
	{{ form_row(form.text) }}
    
	<button type="submit" class="btn btn-primary">Enregistrer</button>
	{{ form_end(form) }}
{% endblock %}
